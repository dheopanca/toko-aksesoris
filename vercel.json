{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install && npm --prefix backend install",
  "framework": "vite",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
